namespace ConductorSharp.Engine.Interface
{
    // This interface should be applied to models coresponding to nameable condutctor objects (e.g workfow, subworkflow, simple tasks)
    public interface INameable
    {
    }
}
